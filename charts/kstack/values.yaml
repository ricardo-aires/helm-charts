# Default values for kstack.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

kafka:
  enabled: true
  zookeeper:
    enabled: true

schema-registry:
  enabled: true

kafka-connect:
  enabled: true

ksqldb:
  enabled: false

kafdrop:
  enabled: false

kafka-rest:
  enabled: false
